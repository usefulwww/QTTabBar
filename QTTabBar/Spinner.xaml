<UserControl x:Class="QTTabBarLib.Spinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Height="23" Width="50">
        <TextBox x:Name="txtValue"
                 KeyUp="txtValue_KeyUp"
                 MouseLeave="txtValue_MouseLeave"
                 Padding="0,0,16,0"
                 Text="0"       
                 TextChanged="txtValue_TextChanged"/>        
        <ScrollBar x:Name="scrollBar" 
                   HorizontalAlignment="Right"
                   Margin="1"
                   Maximum="10000"
                   Minimum="0"
                   SmallChange="1"
                   Value="0"
                   ValueChanged="scrollBar_ValueChanged">
            <ScrollBar.LayoutTransform>
                <RotateTransform 
                    Angle="180" />
            </ScrollBar.LayoutTransform>
        </ScrollBar>        
    </Grid>
</UserControl>